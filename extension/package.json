{
  "name": "offeru-job-collector",
  "version": "0.1.0",
  "private": true,
  "description": "OfferU 岗位采集插件 — 从 Boss直聘等招聘平台一键采集岗位到 OfferU",
  "scripts": {
    "build": "tsc && node scripts/copy-static.mjs",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.5.0"
  }
}
